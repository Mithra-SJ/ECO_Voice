"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AWSSecretsManagerUtils = void 0;
const client_secrets_manager_1 = require("@aws-sdk/client-secrets-manager");
const PREFIX = '\x1b[34m[AWS_SECRET_MANAGER]\x1b[0m';
class AWSSecretsManagerUtils {
    constructor(options = {}, client) {
        this.silent = options.silent ?? false;
        this.secretId = options.secretId ?? process.env.EI_AWS_SECRET_ID ?? '';
        this.secretName = options.secretName ?? process.env.EI_AWS_SECRET_NAME ?? '';
        // Allow dependency injection of the AWS client for testing/mocking
        this.client = client ?? new client_secrets_manager_1.SecretsManagerClient({
            region: options.region ?? process.env.AWS_REGION
        });
    }
    async getSecret() {
        // Don't proceed if required values are missing
        if (!this.secretId || !this.secretName) {
            this.logError(`ERROR - Missing required env variables EI_AWS_SECRET_ID and/or EI_AWS_SECRET_NAME`);
            return undefined;
        }
        try {
            const response = await this.client.send(new client_secrets_manager_1.GetSecretValueCommand({ SecretId: this.secretId }));
            const secretString = response.SecretString;
            if (!secretString) {
                this.logError('ERROR - Secret value is empty');
                return undefined;
            }
            const secretData = JSON.parse(secretString);
            const secretValue = secretData[this.secretName];
            if (!secretValue) {
                this.logError(`ERROR - Secret not found with name: ${this.secretName}`);
                return undefined;
            }
            this.logInfo('Secret successfully retrieved from AWS Secrets Manager');
            return secretValue;
        }
        catch (error) {
            this.logError(`ERROR - Unable to retrieve secret from AWS Secrets Manager: ${error instanceof Error ? error.message : String(error)}`);
            return undefined;
        }
    }
    logInfo(message) {
        if (!this.silent) {
            console.log(PREFIX, message);
        }
    }
    ;
    logError(message) {
        console.error(PREFIX, message);
    }
    ;
}
exports.AWSSecretsManagerUtils = AWSSecretsManagerUtils;
//# sourceMappingURL=aws-sm-utils.js.map