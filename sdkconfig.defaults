# ECO_Voice — ESP-IDF SDK defaults

# Partition table (REQUIRED for esp-sr model auto-flash)
CONFIG_PARTITION_TABLE_CUSTOM=y
CONFIG_PARTITION_TABLE_CUSTOM_FILENAME="partitions.csv"

# Flash size: 8MB
CONFIG_ESPTOOLPY_FLASHSIZE_8MB=y
CONFIG_ESPTOOLPY_FLASHSIZE="8MB"

# Serial console — USB-JTAG (COM8, "USB Serial Device" port on ESP32-S3 DevKitC-1)
# This routes ESP_LOG output to the native USB port, not UART0 GPIO43/44.
CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y
CONFIG_USJ_ENABLE_USB_SERIAL_JTAG=y
CONFIG_ESP_CONSOLE_SECONDARY_NONE=y

# app_main stack — must be large enough for ESP-SR model init
CONFIG_ESP_MAIN_TASK_STACK_SIZE=16384

# Wake word: "Hi ESP"  ← say this to wake the device (replaces "ECO")
CONFIG_SR_WN_WN9_HIESP=y

# Command recognition: English MultiNet 7
CONFIG_SR_MN_EN_MULTINET7_QUANT=y
