/*
 * secrets.h.example — Template for credentials. This file IS tracked in git.
 *
 * SETUP:
 *   cp secrets.h.example secrets.h
 *   Edit secrets.h and change SECRET_CODE to your own phrase.
 *   secrets.h is .gitignored and will never be committed.
 */

#ifndef SECRETS_H
#define SECRETS_H

#define SECRET_CODE        "Alohomora"
// Spoken form of SECRET_CODE registered with ESP-SR MultiNet.
// Must be a pronounceable English phrase the model can recognize.
// Change both values together if you update the secret code.
#define SECRET_CODE_PHRASE "alohomora"

#endif // SECRETS_H
