# ECO_Voice Testing Branch — main component
# Note: Arduino framework (PlatformIO) auto-discovers sources.
# This file is kept for reference only and is not used by the Arduino build.

set(app_sources
    "main.cpp"
    "sensor_handler.cpp"
    "appliance_control.cpp"
)
